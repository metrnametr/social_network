const primaryColor = '#00BCD4';
const linkColor = '#1890ff';
const successColor = '#009688';
const warningColor = '#faad14';
const errorColor = '#E91E63';
const fontSizeBase = '14px';
const headingColor = 'rgba(0, 0, 0, 0.85)';
const textColor = 'rgba(0, 0, 0, 0.65)';
const textColorSecondary  = 'rgba(0, 0, 0, .45)';
const disabledColor  = 'rgba(0, 0, 0, .25)';
const borderRadiusBase = '4px';
const borderColorBase = '#9E9E9E';
const boxShadowBase = '0 2px 8px rgba(0, 0, 0, 0.15)';

export {
  primaryColor,
  linkColor,
  successColor,
  warningColor,
  errorColor,
  fontSizeBase,
  headingColor,
  textColor,
  textColorSecondary,
  disabledColor,
  borderRadiusBase ,
  borderColorBase ,
  boxShadowBase,
};